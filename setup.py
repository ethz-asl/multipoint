from setuptools import setup, find_packages

setup(
    name='multipoint',
    version='1.0.0',
    description='A neural network and the utilities to detect and describe keypoints in optical and thermal images',
    author='Achermann Florian',
    author_email='florian.achermann@mavt.ethz.ch',
    packages=find_packages(),
)
